extends ../layout
block content

  h1= article.title
    div
      var date = new Date(article.published).toISOString().slice(0, 19).replace('T', ' ');
      small Posted: #{date}
    div!= article.body